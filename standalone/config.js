// GitHub Music Player Configuration
// Update these values to point to your GitHub repository

const CONFIG = {
    // Your GitHub repository in format: username/repo-name
    GITHUB_REPO: 'YOUR_USERNAME/YOUR_REPO',

    // The branch where your music files are stored
    GITHUB_BRANCH: 'main',

    // The folder in your repo that contains MP3 files (default: music)
    MUSIC_FOLDER: 'music'
};
